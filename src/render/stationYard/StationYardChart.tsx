/** STUB — owned by the render stream. Replace the body, keep the props type. */
import { TID } from '@e2e/testids';
import type { StationYardProps } from '../types';

export function StationYardChart(_props: StationYardProps) {
  return <div data-testid={TID.yardChart} />;
}
